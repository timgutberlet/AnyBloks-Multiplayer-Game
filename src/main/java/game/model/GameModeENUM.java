package game.model;

/**
 * Enum that stores the different types of game modes which can be played.
 *
 * @author tiotto
 */

/*
let's use a specific class for each one, so we can store additional data to the gamemodes
see gamemodes foldes
 */
public enum GameModeENUM {
  CLASSIC, DUO, JUNIOR, TRIGON;
}
